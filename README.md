# netcore-ldap-authentication

Work-around to authenticate user credentials against an Active Directory using Novell.Directory.Ldap.NETStandard until `System.DirectoryServices` are supported in .NET Core.
